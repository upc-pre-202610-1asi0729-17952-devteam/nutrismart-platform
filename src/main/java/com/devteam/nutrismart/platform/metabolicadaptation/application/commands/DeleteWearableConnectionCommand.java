package com.devteam.nutrismart.platform.metabolicadaptation.application.commands;

/**
 * Comando para eliminar una conexión wearable existente identificada por su {@code id}.
 */
public record DeleteWearableConnectionCommand(Long id) {}
